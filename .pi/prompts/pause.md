---
description: Pause the current lesson and bank progress
---
Pause the current lesson where we are. Load the `learning-teach` skill and run the pause protocol: an exit ticket for today's checkpoints only, a partial lesson file with Status + Resume-from pointer, and a partial `Pending Ingest.json`. Then run the Ingest flow via the `clerk` subagent to bank today's progress (this keeps the Scout digest and the lesson in-progress).
